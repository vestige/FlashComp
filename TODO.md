# list
- [] ジム管理画面の動線変更　　　
- [] データ入出力・運用: スケール面の見積もりと改善（Firestore読み取り最適化）
- [] デザイン変更。tailwindcssを利用する

## 明日の実装計画（別マシン再開用）
1. 事前準備
   - `git pull origin main`
   - `npm ci`
   - 必要なら `npm run db:reset`（READMEの認証環境変数を設定）
2. データモデル移行の本実装
   - [x] 採点画面を `tasks + categoryTaskMap(assignments)` 直接参照に変更
   - [x] ランキング画面を `tasks + assignments` 参照に変更
   - [x] CSV出力（順位系）を新モデル参照へ変更
   - [x] 旧 `routes` 依存箇所を洗い出し（`rg "routes"`）し、段階的に撤去
3. 命名整理（可読性）
   - [x] UI文言を `participants` -> `climbers` 相当に統一
   - [x] ドキュメント表記を `users(=gymOwners)` として統一
4. 管理画面の導線（3入口）
   - [x] 設定（シーズン/カテゴリ/課題）
   - [x] 参加者登録
   - [x] スコア（採点/CSV/公開ランキング）
   - [x] Dashboard と EditEvent のリンク整理
5. 受け入れ確認
   - [x] `npm run lint`
   - [x] `npm run test:run`
   - [x] `npm run build`
   - [x] SPEC.md/README.md/TODO.md の更新反映を確認

# spec
## 目的
* 運営側の負担を減らす（点数設定、集計、公開）
* 参加者のモチベーションを上げる（すぐに結果がみれる）

## できること
### システム管理者
* ジムのオーナー登録
* ジムの登録・削除・編集
* すべてのジムのイベントなどの編集や、採点なども行える

### 運営側（ジムのオーナー）
#### イベント開催前
* イベントの登録・削除・編集（イベント名、設定するシーズン数、カテゴリー）
* シーズンの登録・削除・編集（シーズン名）
   * シーズンは複数設定することが可能
* シーズンごとの課題の登録・削除・編集（番号、級、点数）
* カテゴリーの登録・削除・編集（カテゴリー名）

#### イベント開催後
* 参加者の登録（名前、会員番号、年齢、性別、参加するカテゴリー）
* CSV形式での各データの入出力（統計データとして、参加者や、男女比率や、シーズンごとの順位など）
* 参加者ごとの得点入力。登れた課題をチェックする（このときはスマートフォンでできるといい）

### 参加者側
* スマートフォンで閲覧する
* ログインは今の時点では不要にしたい
* 参加者の個別スコアが確認できる
* 集計結果の確認（カテゴリごとの順位）
