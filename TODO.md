# TODO

## 次の実装順（2026-03-03 見直し）
- [x] P0 整合性: イベント期間変更時、既存シーズンが範囲外にならない検証
- [x] P0 UX: `Scores` 画面で season/category 選択状態を戻り遷移でも保持
- [x] P1 UX: クライマー向けページのイベント一覧から TOP に戻る導線を追加
- [x] P1 安全性: 削除Confirmを専用モーダルに変更（誤操作防止）
- [x] P1 性能: Firestore最適化第2弾（`EventDataIO` / `ParticipantScoreDetail` の読み取り削減）
- [x] P2 性能: ビルド警告の大きいチャンク対策（ルート単位の lazy 分割）
- [x] P2 UI: デザインをもっと洗練
- [x] P2 UI(Phase1): クライマー向け3画面（`ScoreSummary` / `EventSummary` / `ParticipantScoreDetail`）のトーン統一
- [x] P2 UI(Phase2): 管理画面（`Dashboard` / `EditEvent` / `SeasonEdit` ほか）のトーン統一
- [x] 受け入れ確認: `npm run lint` / `npm run test:run` / `npm run build`

## 気になること
- [ ] 各ページにヘッダーとフッターを用意して共通にする（topページに合わせる）
- [ ] 課題登録画面で保存やチェックボックスにチェックを入れるたびにメッセージが上に表示されるため、画面が動いた感じがしていやだ
- [ ] テストデータをバリエーションよく用意したい
- [ ] 実際に運用する前にデータの全消去、運用開始時のバックアップ手順などを用意しておきたい

## 完了済み
- [x] シーズン編集画面にイベントの期間を表示する
- [x] シーズンを登録している状態でイベントを編集するときに、範囲外に設定するときに、シーズンの設定がおかしいことがイベント編集画面のシーズンカードでわかるようにする
- [x] イベント編集画面の「現在の登録状況を見ながら、上から順に設定するとスムーズです。」カードを削除
- [x] CSV出力をクライマー管理とScore管理に分離（別画面を廃止）
- [x] セキュリティ: `users` コレクション読み取り権限を最小化（self/adminのみ）
- [x] セキュリティ: `useOwnerProfile` のメールベース自動admin付与を撤廃
- [x] 整合性: イベント/シーズン/カテゴリ/クライマー削除時の関連データ整理（カスケード）
- [x] 整合性: クライマーのカテゴリ変更時にシーズン別スコアのカテゴリ不整合を整理
- [x] 不正操作対策: `ScoreInput` で `participant.categoryId` とURL `categoryId` の一致検証
- [x] 日付安全化: `YYYY-MM-DD` 保存時のタイムゾーンずれ防止（ローカル日付として保存）
- [x] 仕様一致: CSVインポート必須列（`categoryId`）の実装と表示文言の一致
